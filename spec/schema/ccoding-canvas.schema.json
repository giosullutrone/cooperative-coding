{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cooperativecoding.dev/schema/ccoding-canvas.schema.json",
  "title": "CooperativeCoding Canvas Metadata",
  "description": "JSON Schema for validating the top-level ccoding metadata object in a JSON Canvas file.",
  "type": "object",
  "properties": {
    "specVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of the CooperativeCoding spec this canvas conforms to (MAJOR.MINOR.PATCH)."
    },
    "language": {
      "type": "string",
      "description": "Default programming language for all nodes in this canvas. Individual nodes may override.",
      "examples": ["python", "typescript", "rust", "go", "java", "csharp"]
    }
  },
  "required": ["specVersion"],
  "additionalProperties": true
}
